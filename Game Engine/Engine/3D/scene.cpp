//
//  scene3d.cpp
//  game engine
//
//  Created by Indi Kernick on 7/09/2016.
//  Copyright © 2016 Indi Kernick. All rights reserved.
//

#include "scene.hpp"

/*ID::Type D3::Scene::add(const Mesh &mesh) {
  ID::Type id = idGen.create();
  meshs.emplace(id, mesh);
  return id;
}

D3::Mesh& D3::Scene::get(ID::Type id) {
  auto iter = meshs.find(id);
  if (iter == meshs.end()) {
    throw std::invalid_argument("No mesh with that ID was found");
  } else {
    return iter->second;
  }
}

void D3::Scene::rem(ID::Type id) {
  meshs.erase(id);
}*/
